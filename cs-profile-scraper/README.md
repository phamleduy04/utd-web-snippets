## UTD CS Profile Scraper

This is a simple script to scrape [CS Faculty List](https://cs.utdallas.edu/people/faculty/) and parse the data into a csv file.

### Usage
0. Requires Node.js
1. Clone the repository and cd into the directory (cd cs-profile-scraper)
2. Install dependencies `npm install`
3. Run the script `node index.js`

### Output
The output will be saved to `output.csv`

